User-agent: *
Allow: /

# Authenticated surfaces are never indexed.
Disallow: /portal
Disallow: /admin

# A developer reference rather than a page anyone should find in search results.
Disallow: /design-system

# Absolute sitemap URL requires a confirmed public origin. Indexed public routes include
# /, /about, /inclusion, /employers, /job-seekers, /our-solutions, /benefits, /resources,
# and /contact. /design-system, /portal, and /admin stay out of the index.
